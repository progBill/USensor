<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.billebeling.usensor.sensorreader" >

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
﻿
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="info.billebeling.usensor.sensorreader.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="info.billebeling.usensor.sensorreader.DetailsActivity"
            android:label="@string/title_activity_details"
            android:parentActivityName="info.billebeling.usensor.sensorreader.MainActivity" >
        </activity>
        <service android:name="info.billebeling.usensor.sensorreader.SensorWrangler" />

    </application>

</manifest>
